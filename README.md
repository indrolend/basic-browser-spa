this entire code base is my attempt to refactor an mpa that i made, into an SPA. i don't know much about coding, so most of the code was done through sprints with prompted github copilot agent session pull requests. i think my code is bloated, disjointed, overly complex, so i wanted to take what i've made so far and make a braindead simple SPA following the standard boiler plate rules and usual SPA architecture concepts, avoiding code drift and regaining control over a neater and tighter system. the "js" folder contains most of the spa that i developed up until this point, and it is mostly working, although i suspect it may be over complicated or bloated. "particle carousel" is a code base that i developed to use in the SPA for a transition effect. the "particle carousel" mostly focuses on gifs, but i want to have the animation and functionality as one of the main transition motifs in the SPA. but the issue has been parallel development, and not knowing specifically how to implement the particle carousel code, what to implement from it. so i want to return to simplicity and make a simple SPA from what i already have, with more intent around using the useful parts of the 'particle carousel" codebase.

